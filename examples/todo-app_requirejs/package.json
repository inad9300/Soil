{
    "private": true,
    "dependencies": {
        "@soil/dom": "0.6.6",
        "@soil/arch": "0.6.6"
    },
    "devDependencies": {
        "http-server": "0.11.1",
        "requirejs": "2.3.6",
        "typescript": "3.1.6"
    },
    "scripts": {
        "precompile": "rm -rf build",
        "compile": "tsc",
        "prestart": "npm run compile",
        "start": "http-server"
    }
}
