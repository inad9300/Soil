{
    "dependencies": {
        "@soil/web": "0.1.0"
    },
    "devDependencies": {
        "http-server": "0.11.1",
        "requirejs": "2.3.5",
        "typescript": "2.6.2"
    },
    "scripts": {
        "precompile": "rm -rf build",
        "compile": "tsc",
        "prestart": "npm run compile",
        "start": "http-server"
    }
}
