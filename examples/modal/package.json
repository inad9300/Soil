{
    "dependencies": {
        "@soil/dom": "0.6.6",
        "@soil/arch": "0.6.6"
    },
    "devDependencies": {
        "http-server": "0.11.1",
        "browserify": "16.2.3",
        "typescript": "3.1.6"
    },
    "scripts": {
        "precompile": "rm -rf build",
        "compile": "tsc",
        "bundle": "browserify -e build/main.js -o build/app.js -d",
        "prestart": "npm run compile && npm run bundle",
        "start": "http-server"
    }
}
